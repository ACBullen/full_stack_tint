import React from 'react';
import SessionFormContainer from './session_form_container';

const Main = ({store}) => (
  <div>
    <header>
      <h1>T.I.N.T.</h1>
    </header>
    <SessionFormContainer />
  </div>
)

export default Main;
